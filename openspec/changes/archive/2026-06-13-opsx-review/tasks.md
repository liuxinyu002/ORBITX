## 1. 创建评审引擎 Skill

> **目标**: 创建 `/opsx:review` 命令的完整 Skill 定义文件。
> **依赖**: 无
> **验证**: 执行 `/opsx:review` 后输出符合 Few-Shot 模板的结构化报告；未授权时不修改文件，授权后直接回写原方案文件。

- [x] 1.1 创建 `.claude/skills/openspec-review/` 目录结构
- [x] 1.2 编写 SKILL.md System Prompt——定义 6 维审核清单，每条含检查逻辑与严重度
- [x] 1.3 嵌入 Few-Shot 标准输出示例，锁定 Markdown 报告结构（全局摘要 + 下一步动作提示 + 仅展开问题维度 + 行首 `- [ ] severity` checklist）
- [x] 1.4 嵌入修复建议颗粒度硬约束：只允许输出配置片段/架构伪代码/Schema 变更描述，严禁完整业务逻辑代码
- [x] 1.5 实现活跃变更自动检测逻辑：扫描 `openspec/changes/`（排除 `archive/`），多变更时展示带摘要的候选列表
- [x] 1.6 实现默认只读约束：未获授权时禁止修改 proposal/design/specs/tasks 文件
- [x] 1.7 实现修改预览：授权回写前先展示拟修改文件与修改摘要
- [x] 1.8 实现授权回写粒度控制：支持仅修复 Blocker、修复 Blocker + Warning、逐条确认
- [x] 1.9 实现授权回写：用户确认范围后，直接修改对应的 proposal/design/specs/tasks 文件
- [x] 1.10 创建 `.claude/commands/opsx/review.md` 命令注册文件，使 `/opsx:review` 作为可调用命令

## 2. 授权回写验证

> **目标**: 验证 `/opsx:review` 在未授权和已授权两种模式下都遵守边界，且修改直接落在原方案文件中。
> **依赖**: Stage 1
> **验证**: 未授权时仓库无方案文件变更；授权后仅相关原方案文件被最小修改。

- [x] 2.1 验证未授权时 `/opsx:review` 仅输出诊断结果，不修改任何方案文件
- [x] 2.2 验证授权前先出现”拟修改文件 + 修改摘要”预览
- [x] 2.3 验证选择”仅修复 Blocker”时不会落盘 Warning/Suggestion 对应修改
- [x] 2.4 验证选择”修复 Blocker + Warning”时仅落盘这两类修改
- [x] 2.5 验证选择”逐条确认”时可跳过单个建议
- [x] 2.6 验证授权后 `/opsx:review` 仅修改与问题直接相关的方案文件
- [x] 2.7 验证不会新增 `review.md` 或其他中间状态文件
- [x] 2.8 验证既有 `/opsx:apply` Skill 文件保持不变

## 3. 端到端验证

> **目标**: 在 Phase 4 变更上运行完整评审流程，验证各维度审查正常、输出格式正确、授权回写生效。
> **依赖**: Stage 1 + Stage 2
> **验证**: 所有场景通过。

- [x] 3.1 在 Phase 4 变更上运行 `/opsx:review`，验证 6 维度全部执行、输出格式符合模板
- [x] 3.2 验证通过维度不逐条展示，仅在摘要区显示通过维度数量
- [x] 3.3 验证修复建议颗粒度：确认所有建议均为配置/伪代码/Schema 级别，无业务逻辑代码
- [x] 3.4 验证未授权时 Phase 4 原方案文件保持不变
- [x] 3.5 验证摘要区包含明确下一步动作提示
- [x] 3.6 验证多变更场景下候选列表包含变更名、最近修改时间和一句话摘要
- [x] 3.7 验证授权后问题被直接修正到 `proposal/design/specs/tasks` 对应文件
- [x] 3.8 验证修改完成后输出变更回执，清楚列出已改/未改问题
- [x] 3.9 验证全流程无需修改 `/opsx:apply` 规则即可继续进入实施阶段
